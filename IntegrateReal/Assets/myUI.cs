using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UIElements;

/* This script contains the controls for the on press buttons*/

public class myUI : MonoBehaviour
{
    /*public capsuleController capsule;

    private void OnEnable
    {
        VisualElement root = GetComponent<UIDocument>().rootVisualElement;

        Button buttonUp = root.Q<Button>("ButtonUp");
        Button buttonLeft = root.Q<Button>("ButtonLeft"); 
        Button buttonX = root.Q<Button>("ButtonX"); //Stop button
        Button buttonRight = root.Q<Button>("ButtonRight");
        Button buttonDown = root.Q<Button>("ButtonDown");

        buttonUp.clicked += () => capsule.
        buttonLeft.clicked += () => capsule.
        buttonX.clicked += () => capsule.
        buttonRight.clicked += () capsule.
        buttonDown.clicked += () capsule.

    }*/
}
